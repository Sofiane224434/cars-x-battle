services:
  db:
    image: mysql:8.0
    container_name: cxb-db
    restart: unless-stopped
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootsecret}
      MYSQL_DATABASE: ${DB_NAME:-cars_x_battle}
      MYSQL_USER: ${DB_USER:-cxb_user}
      MYSQL_PASSWORD: ${DB_PASSWORD:-cxb_pass}
    volumes:
      - dbdata:/var/lib/mysql
      - ./backend/schema.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    networks:
      - cxb-network
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u root -p$${MYSQL_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cxb-api
    restart: unless-stopped
    environment:
      PORT: 5000
      NODE_ENV: production
      DB_HOST: db
      DB_USER: ${DB_USER:-cxb_user}
      DB_PASSWORD: ${DB_PASSWORD:-cxb_pass}
      DB_NAME: ${DB_NAME:-cars_x_battle}
      JWT_SECRET: ${JWT_SECRET:-cxb_default_secret_key_change_me}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      BREVO_API_KEY: ${BREVO_API_KEY:-}
      BREVO_SENDER_EMAIL: ${BREVO_SENDER_EMAIL:-}
      BREVO_SENDER_NAME: ${BREVO_SENDER_NAME:-Cars x Battle}
      DEEPL_API_KEY: ${DEEPL_API_KEY:-}
      APP_URL: ${APP_URL:-https://cxb.azim404.com}
      CORS_ORIGIN: ${CORS_ORIGIN:-https://cxb.azim404.com}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - cxb-network

  app:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: cxb-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:3013:80"
    depends_on:
      - api
    networks:
      - cxb-network

volumes:
  dbdata:

networks:
  cxb-network:
    driver: bridge
